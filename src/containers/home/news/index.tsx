import classNames from "./news.module.scss";

const News = () => {
  return (
    <section id="news" className={classNames.main}>
      <div className={classNames.content}>a</div>
    </section>
  );
};

export default News;
