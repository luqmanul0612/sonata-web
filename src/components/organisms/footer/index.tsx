import classNames from "./footer.module.scss";

const Footer = () => {
  return (
    <div className={classNames.main}>
      <div className={classNames.content}>Footer</div>
    </div>
  );
};

export default Footer;
